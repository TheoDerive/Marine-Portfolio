type categorieType = "competance" | "projet" | "review";

const useFetch = {
  GET: (categorie: categorieType) => {
    // const upperCaseCategorie = categorie.map((letter: string, index: number) =>
    //   console.log(index, letter),
    // );
    // const response = fetch(`/api/${categorie}/get${}`)
  },
};

export default useFetch;
